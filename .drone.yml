kind: pipeline
type: ssh
name: default

server:
  host: 596fe1f7.ngrok.io
  user: root
  password: thisisasecret

steps:
- name: backend
  commands:
  # Set the M2_HOME EV  
  - export M2_HOME=/usr/share/maven
  # Run a maven command
  - mvn -version
    